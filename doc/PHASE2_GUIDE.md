# Phase 2 完成指南

## 📋 概述

Phase 2 实现了 mini-ray 的**任务调度和执行系统**，包括：

- ✅ **Scheduler（调度器）**：任务队列管理和 Worker 分配
- ✅ **CoreWorker（核心工作器）**：任务提交和执行的核心组件
- ✅ **Worker 进程**：独立的任务执行进程
- ✅ **Python API**：用户友好的 API 接口（`@ray.remote`、`ray.get()`）
- ✅ **完整测试**：验收测试和示例代码

---

## 🏗️ 架构说明

### 组件关系

```
用户代码
  ↓
Python API (@ray.remote, ray.get)
  ↓
CoreWorker (Python 绑定)
  ↓
Scheduler (C++) ← → Worker 进程
  ↓
ObjectStore (C++)
```

### 工作流程

1. **初始化**：`ray.init(num_workers=2)` 启动系统和 Worker 进程
2. **定义远程函数**：使用 `@ray.remote` 装饰器
3. **提交任务**：调用 `func.remote(args)` 返回 ObjectRef
4. **Worker 执行**：
   - Worker 从 Scheduler 获取任务
   - 反序列化函数和参数
   - 执行函数
   - 序列化结果并存储到 ObjectStore
5. **获取结果**：`ray.get(object_ref)` 从 ObjectStore 读取结果
6. **关闭**：`ray.shutdown()` 清理资源

---

## 🔨 编译和构建

### 前置条件

- Python 3.7+
- CMake 3.15+
- pybind11
- C++14 编译器

### 编译步骤

```bash
# 1. 进入项目根目录
cd /Users/nedonion/PycharmProjects/mini-ray

# 2. 编译 C++ 扩展模块
python3 setup.py build_ext --inplace

# 3. 验证编译结果
# 应该生成 python/miniray/_miniray_core.*.so 文件
ls -l python/miniray/_miniray_core*.so
```

### 可能的编译问题

**问题 1**: 找不到 pybind11
```bash
# 安装 pybind11
pip install pybind11
```

**问题 2**: CMake 版本太低
```bash
# macOS
brew upgrade cmake

# Ubuntu
sudo apt-get install cmake
```

**问题 3**: C++ 编译器不支持 C++14
```bash
# 检查编译器版本
g++ --version  # 需要 GCC 5+
clang++ --version  # 需要 Clang 3.4+
```

---

## 🧪 运行测试

### 验收测试

验收测试包含 7 个测试用例，全面验证 Phase 2 功能：

```bash
# 运行完整的验收测试
python3 test_phase2.py
```

**测试内容**：
1. ✅ 基本远程函数调用
2. ✅ 多任务并发执行
3. ✅ 复杂计算任务
4. ✅ 字符串操作
5. ✅ 复杂数据结构（列表、字典）
6. ✅ Worker 负载均衡
7. ✅ 错误处理

**期望输出**：
```
======================================================================
                        Phase 2 验收测试
======================================================================

初始化 mini-ray (2 个 Worker)...
✓ 初始化成功

...

======================================================================
                         测试总结
======================================================================

总计: 7 个测试
通过: 7 个
失败: 0 个

🎉 所有测试通过！
```

### 单元测试（已有）

```bash
# 运行 Phase 1 的单元测试
python3 test_miniray.py
```

---

## 📚 示例代码

Phase 2 提供了 3 个详细的示例程序：

### 1. 基础任务示例

**文件**: `examples/phase2_01_basic_tasks.py`

**内容**：
- 简单的远程函数
- 提交多个任务
- 不同的数据类型
- 计算密集型任务
- 任务依赖（手动编排）

**运行**：
```bash
python3 examples/phase2_01_basic_tasks.py
```

**学习要点**：
- 理解 `@ray.remote` 装饰器
- 理解 `.remote()` 和 `ray.get()` 的使用
- 理解 ObjectRef 的概念

---

### 2. 并行计算示例

**文件**: `examples/phase2_02_parallel_computation.py`

**内容**：
- Monte Carlo 模拟计算 π
- 并行数据处理
- MapReduce 模式（词频统计）
- 任务并行

**运行**：
```bash
python3 examples/phase2_02_parallel_computation.py
```

**学习要点**：
- 数据并行（Data Parallelism）
- 任务并行（Task Parallelism）
- MapReduce 模式
- 任务分解和结果合并

---

### 3. 性能对比示例

**文件**: `examples/phase2_03_performance_comparison.py`

**内容**：
- CPU 密集型任务对比
- 扩展性测试
- 开销分析
- 任务粒度分析
- Amdahl 定律演示

**运行**：
```bash
python3 examples/phase2_03_performance_comparison.py
```

**学习要点**：
- 加速比（Speedup）
- 并行效率（Efficiency）
- 远程调用开销
- 任务粒度选择
- Amdahl 定律

---

## 💡 使用指南

### 基本用法

```python
import miniray as ray

# 1. 初始化系统
ray.init(num_workers=2)

# 2. 定义远程函数
@ray.remote
def add(a, b):
    return a + b

# 3. 调用远程函数
result_ref = add.remote(3, 5)

# 4. 获取结果
result = ray.get(result_ref)
print(result)  # 输出: 8

# 5. 关闭系统
ray.shutdown()
```

### 并行处理多个任务

```python
import miniray as ray
import time

ray.init(num_workers=4)

@ray.remote
def square(x):
    return x * x

# 提交多个任务
refs = [square.remote(i) for i in range(10)]

# 等待执行
time.sleep(2)

# 获取所有结果
results = [ray.get(ref) for ref in refs]
print(results)  # [0, 1, 4, 9, 16, 25, 36, 49, 64, 81]

ray.shutdown()
```

### 处理复杂数据类型

```python
import miniray as ray

ray.init(num_workers=2)

@ray.remote
def process_data(data):
    """处理字典数据"""
    return {k: v * 2 for k, v in data.items()}

# 传递字典
ref = process_data.remote({"a": 1, "b": 2, "c": 3})
result = ray.get(ref)
print(result)  # {"a": 2, "b": 4, "c": 6}

ray.shutdown()
```

---

## 📖 API 参考

### `ray.init(num_workers=2)`

初始化 mini-ray 系统。

**参数**：
- `num_workers` (int): Worker 进程数量，默认为 2

**返回**：None

**示例**：
```python
ray.init(num_workers=4)  # 启动 4 个 Worker
```

---

### `@ray.remote`

将普通函数转换为远程函数的装饰器。

**用法**：
```python
@ray.remote
def my_function(arg1, arg2):
    return arg1 + arg2
```

**返回**：`RemoteFunction` 对象

---

### `RemoteFunction.remote(*args, **kwargs)`

提交远程函数调用，立即返回 ObjectRef。

**参数**：
- `*args`: 位置参数
- `**kwargs`: 关键字参数

**返回**：`ObjectRef` - 对象引用

**示例**：
```python
ref = my_function.remote(1, 2)
```

---

### `ray.get(object_ref)`

获取 ObjectRef 对应的实际值（阻塞操作）。

**参数**：
- `object_ref` (ObjectRef): 对象引用

**返回**：对象的实际值

**示例**：
```python
result = ray.get(ref)
```

---

### `ray.shutdown()`

关闭 mini-ray 系统，清理资源。

**参数**：None

**返回**：None

**示例**：
```python
ray.shutdown()
```

---

## 🔍 核心概念

### ObjectRef（对象引用）

- **定义**：远程对象的引用，类似于 "future" 或 "promise"
- **特点**：轻量级，可以快速返回
- **用途**：异步编程，允许提交任务后继续执行其他代码

### 远程函数（Remote Function）

- **定义**：使用 `@ray.remote` 装饰的函数
- **执行位置**：在 Worker 进程中执行，而不是主进程
- **优势**：并行计算、分布式处理

### Task（任务）

- **定义**：一次远程函数调用
- **生命周期**：提交 → 调度 → 执行 → 完成
- **状态**：PENDING（等待）→ RUNNING（执行中）→ FINISHED（完成）

### Worker（工作进程）

- **定义**：独立的 Python 进程，负责执行任务
- **职责**：从 Scheduler 获取任务、执行、存储结果
- **数量**：由 `ray.init(num_workers=N)` 指定

---

## ⚠️ 限制和注意事项

### Phase 2 的限制

1. **不支持自动依赖追踪**
   - 不能将 ObjectRef 作为参数传递给远程函数
   - 需要手动调用 `ray.get()` 获取值后再传递

   ```python
   # ❌ 不支持（Phase 2）
   ref1 = func1.remote()
   ref2 = func2.remote(ref1)  # 错误！

   # ✅ 正确做法
   ref1 = func1.remote()
   value1 = ray.get(ref1)
   ref2 = func2.remote(value1)
   ```

2. **简化的错误处理**
   - 当前实现将异常序列化后返回
   - 未来版本会改进异常传播机制

3. **固定的调度策略**
   - 只实现了 FIFO（先进先出）调度
   - 未来可扩展为优先级调度、公平调度等

### 性能考虑

1. **远程调用有开销**
   - 序列化/反序列化
   - 任务调度
   - 进程间通信
   - **建议**：只在计算时间 >> 开销时使用

2. **任务粒度**
   - 太小：开销 > 收益
   - 太大：负载不均衡
   - **建议**：每个任务耗时 100ms 以上

3. **数据传输**
   - 减少大对象传输
   - 优先传递引用而不是值
   - **建议**：计算在数据所在位置

---

## 🐛 常见问题

### Q1: 编译失败，找不到 pybind11

**解决方案**：
```bash
pip install pybind11
```

### Q2: Worker 进程没有启动

**检查**：
```python
import time
ray.init(num_workers=2)
time.sleep(2)  # 给 Worker 启动时间
```

### Q3: ray.get() 一直阻塞

**可能原因**：
- Worker 还没执行完任务
- Worker 进程崩溃

**调试**：
- 增加等待时间
- 检查 Worker 输出

### Q4: 序列化错误

**原因**：某些对象无法序列化（如文件句柄、锁）

**解决**：
- 只传递可序列化的对象
- 使用 pickle 测试：`pickle.dumps(obj)`

---

## 📝 C++ 代码注释说明

所有 C++ 代码都包含详细的中文注释，包括：

### 语法说明
- `class`、`namespace`、`public/private`
- `std::queue`、`std::mutex`、`std::shared_ptr`
- 构造函数初始化列表
- `std::move` 移动语义
- `->` 指针成员访问

### Python 对比
每个 C++ 概念都有对应的 Python 例子，帮助理解：

```cpp
// C++: 构造函数初始化列表
CoreWorker::CoreWorker(...)
    : scheduler_(std::move(scheduler)),
      object_store_(std::move(object_store)) {
}

// Python 等价：
// def __init__(self, scheduler, object_store):
//     self.scheduler = scheduler
//     self.object_store = object_store
```

### 设计模式
- Dependency Injection（依赖注入）
- RAII（资源获取即初始化）
- Facade（外观模式）

---

## 🎯 下一步：Phase 3

Phase 2 完成后，Phase 3 将实现：

1. **Actor 模型**：有状态的远程对象
2. **自动依赖追踪**：ObjectRef 作为参数
3. **更好的错误处理**：异常传播
4. **高级调度**：优先级调度、公平调度

---

## 📚 学习资源

### 相关概念

- **Future/Promise 模式**：异步编程的基础
- **MapReduce**：分布式数据处理范式
- **Actor 模型**：并发编程模型
- **Amdahl 定律**：并行计算的理论基础

### 推荐阅读

- Ray 官方文档：https://docs.ray.io/
- pybind11 文档：https://pybind11.readthedocs.io/
- C++ 并发编程：《C++ Concurrency in Action》
- 分布式系统：《Designing Data-Intensive Applications》

---

## 📄 文件清单

### C++ 核心

- `cpp/include/miniray/scheduler/scheduler.h` - Scheduler 头文件
- `cpp/src/scheduler/scheduler.cpp` - Scheduler 实现
- `cpp/include/miniray/core_worker/core_worker.h` - CoreWorker 头文件
- `cpp/src/core_worker/core_worker.cpp` - CoreWorker 实现
- `cpp/src/python_bindings.cpp` - Python 绑定（已更新）

### Python 层

- `python/miniray/worker.py` - Worker 进程实现
- `python/miniray/api.py` - Python API（已重写）
- `python/miniray/__init__.py` - 模块导出

### 测试和示例

- `test_phase2.py` - Phase 2 验收测试
- `examples/phase2_01_basic_tasks.py` - 基础任务示例
- `examples/phase2_02_parallel_computation.py` - 并行计算示例
- `examples/phase2_03_performance_comparison.py` - 性能对比示例

### 文档

- `doc/DESIGN.md` - 整体设计文档
- `doc/PHASE2_GUIDE.md` - Phase 2 使用指南（本文档）

---

## ✅ Phase 2 检查清单

- [x] Scheduler C++ 实现
- [x] CoreWorker C++ 实现
- [x] Python 绑定
- [x] Worker 进程
- [x] Python API 层
- [x] 验收测试
- [x] 示例代码
- [x] 详细注释（C++ 和 Python）
- [x] 使用文档

**Phase 2 状态：✅ 完成**

---

## 🙏 致谢

感谢你学习 mini-ray！如果有任何问题或建议，欢迎提出。

**祝你学习愉快！🎉**
